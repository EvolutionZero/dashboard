<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zero.dashboard.mapper.ExtremeMapper">

    <insert id="saveAll">
        INSERT INTO extreme (code, "date", "level", "type", "value")
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.code},#{item.date},#{item.level},#{item.type},#{item.value})
        </foreach>
        ON conflict(code, "date", "level", "type") DO NOTHING
    </insert>

</mapper>
